# vehicle.service — sim on QEMU, SocketCAN on hardware
CO_PILOT_VEHICLE_SOURCE=sim
CO_PILOT_VEHICLE_DEMO=1
CO_PILOT_TELEMETRY_SOCKET=/run/co-pilot/vehicle.sock
# Uncomment to capture decoded CAN frames as MDF4 (requires writable path):
# CO_PILOT_CAN_LOG_PATH=/var/log/co-pilot/can.mf4
